  <div class="price-section">
    <div class="price-info">
      <span class="original-price" id="originalPrice"></span>
      <span class="sale-price" id="salePrice"></span>
      <span class="discount-badge" id="discountBadge"></span>
    </div>
    <div class="per-dose" id="perDose">Only $0.90 Per Dose</div>
  </div>

  <script>
    // Example dynamic update based on URL
    const url = window.location.href;

    // Simple rules for demonstration
    if (url.includes("6-serving-of-whitening-gels-free-starter-kit")) {
        document.getElementById("originalPrice").textContent = "$48";
        document.getElementById("salePrice").textContent = "$19";
        document.getElementById("discountBadge").textContent = "SAVE 60% TODAY";
        document.getElementById("perDose").textContent = "Only $3.1 Per Whitening Session";
    } else {
        document.getElementById("originalPrice").textContent = "$76";
        document.getElementById("salePrice").textContent = "$19";
        document.getElementById("discountBadge").textContent = "SAVE 75% TODAY";
        {% comment %} document.getElementById("perDose").textContent = "Only $0.17 Per Dose"; {% endcomment %}
    }
  </script>

  <style>
    .price-info {
      display: flex;
      align-items: center;
      gap: 10px;
      margin-bottom: 6px;
    }

    .original-price {
      text-decoration: line-through;
      color: gray;
      font-size: 2rem;
    }

    .sale-price {
      font-weight: 700;
      color: #46BDF0;
      font-size: 2.2rem;
    }

    .discount-badge {
      background-color: var(--purple);
      color: white;
      font-weight: bold;
      border-radius: 12px;
      padding: 6px 12px;
      font-size: 1.4rem;
      white-space: nowrap;
    }

    .per-dose {
      color: #46BDF0;
      font-size: 1.4rem;
      font-weight: 600;
    }
  </style>